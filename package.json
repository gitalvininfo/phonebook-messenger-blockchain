{
  "name": "chatwei-lite",
  "version": "1.0.0",
  "description": "A lightweight Ethereum messenger.",
  "main": "truffle.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "truffle deploy --network development --reset"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.3.0"
  }
}
